name: Release

on:
  push:
    branches:
      - main
  workflow_dispatch: # enable manual release

jobs:
  release:
    name: Release
    needs: [lint]
    runs-on: [ubuntu-latest]
    steps:
      - uses: open-turo/actions-tf/release@v2
